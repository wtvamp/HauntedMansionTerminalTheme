{
  "//": "{{ .Name }} — generated by {{ .Generator }}. Merge into settings.json under workbench.colorCustomizations.",
  "workbench.colorCustomizations": {
    "terminal.background": "{{ .UI.background.Hex }}",
    "terminal.foreground": "{{ .UI.foreground.Hex }}",
    "terminalCursor.background": "{{ .UI.cursor_text.Hex }}",
    "terminalCursor.foreground": "{{ .UI.cursor.Hex }}",
    "terminal.selectionBackground": "{{ .UI.selection_background.Hex }}",
{{ range .ANSI }}    "terminal.ansi{{ if ge .Index 8 }}Bright{{ upper (slice .Key 7 8) }}{{ slice .Key 8 }}{{ else }}{{ upper (slice .Key 0 1) }}{{ slice .Key 1 }}{{ end }}": "{{ .Hex }}"{{ if lt .Index 15 }},{{ end }}
{{ end }}  }
}
