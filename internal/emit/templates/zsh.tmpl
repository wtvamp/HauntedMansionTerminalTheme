# {{ .Name }} — generated by {{ .Generator }}. Do not edit; edit palette/haunted-mansion.yaml.
#
# Sourced by the prompt and the greeting. Hex values are here for truecolor
# terminals; the _idx variants are ANSI slot numbers for everything else.
{{ range .ANSI }}typeset -g HM_{{ upper .Key }}='{{ .Hex }}'
typeset -g HM_{{ upper .Key }}_IDX={{ .Index }}
{{ end }}{{ range $k, $e := .UI }}typeset -g HM_UI_{{ upper $k }}='{{ $e.Hex }}'
{{ end }}
# Roles: semantic names the prompt uses, so the prompt never names a color.
{{ range .RoleNames }}typeset -g HM_ROLE_{{ upper . }}={{ (index $.Roles .).Index }}
{{ end }}
